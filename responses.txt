The IP address and DNS are both naming systems for addressing namespaces allocated for entitities in a network.
cookies are a unique tool that servers send to browsers.
HTTP/HTTPS, domain name, and the path leading to a specific web page
